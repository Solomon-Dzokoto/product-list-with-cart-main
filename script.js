const buttons=document.querySelectorAll('.button');

buttons.forEach(button=>
  button.addEventListener('click',()=>{
   const image=buttun.dataset.imageId
   image.style.border='1px solid red'
  };
)
);